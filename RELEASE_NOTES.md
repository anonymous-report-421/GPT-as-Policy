# Source release

No original repository history is included. Deployment-specific mount paths, private image and gateway endpoints were replaced with generic examples. Gateway examples use reserved .invalid domains and require explicit configuration; legacy profile identifiers remain for compatibility. Export sanitization does not change the algorithm, prompts, seeds or scores; media match the supplied verified report build. Report source includes the shared runtime with its existing verification manifest.

Only project-owned code is MIT; see THIRD_PARTY_NOTICES.md. Full videos are published separately in the website repository.
